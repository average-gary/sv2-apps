[package]
name = "jd_client_sv2"
version = "0.4.0"
authors = ["The Stratum V2 Developers"]
edition = "2021"
description = "Job Declarator Client (JDC) role"
documentation = "https://docs.rs/jd_client_sv2"
readme = "README.md"
homepage = "https://stratumprotocol.org"
repository = "https://github.com/stratum-mining/stratum"
license = "MIT OR Apache-2.0"
keywords = ["stratum", "mining", "bitcoin", "protocol"]

[lib]
name = "jd_client_sv2"
path = "src/lib/mod.rs"

[dependencies]
stratum-apps = { version = "0.6.0", path = "../../stratum-apps", features = ["jd_client"] }
async-channel = "1.5.1"
async-trait = { version = "0.1", optional = true }
serde = { version = "1.0.89", default-features = false, features = ["derive", "alloc"] }
tokio = { version = "1.44.1", features = ["full"] }
ext-config = { version = "0.14.0", features = ["toml"], package = "config" }
tracing = { version = "0.1" }
clap = { version = "4.5.39", features = ["derive"] }
hex = "0.4.3"
hotpath = "0.14.0"
# iroh used directly by JDC's outbound connectors (Phase 4b). Pinned to the
# same version stratum-apps depends on.
iroh = { version = "1.0.0-rc.0", default-features = false, features = ["tls-ring"], optional = true }

[dev-dependencies]
toml = "1.1.2"

[features]
default = ["monitoring"]
monitoring = ["stratum-apps/asic-rs-telemetry"]
hotpath = ["hotpath/hotpath"]
hotpath-alloc = ["hotpath", "hotpath/hotpath-alloc"]
iroh-transport = ["stratum-apps/iroh-transport", "dep:async-trait", "dep:iroh"]
